package com.klmpk5.daycare_admin.ui.theme

import androidx.compose.ui.graphics.Color

// Warna default bawaan Material Theme
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650A4)
val PurpleGrey40 = Color(0xFF625B71)
val Pink40 = Color(0xFF7D5260)


// ===============================
// Custom Color Theme Daycare Admin
// ===============================

// Warna utama aplikasi.

val DaycarePrimary = Color(0xFF2A9D8F)

// Warna primary versi lebih terang.
val DaycarePrimaryLight = Color(0xFFDDF4EF)

// Warna primary versi gelap.
val DaycarePrimaryDark = Color(0xFF1F7A70)

// Warna background utama aplikasi.
val DaycareBackground = Color(0xFFF8FBFF)

// Warna background gradient bagian atas.
val DaycareSoftMint = Color(0xFFEAF7F4)

// Warna card atau container utama.
val DaycareCard = Color(0xFFFFFFFF)

// Warna teks utama.
val DaycareTextPrimary = Color(0xFF1F2937)

// Warna teks sekunder.
val DaycareTextSecondary = Color(0xFF6B7280)

// Warna teks yang lebih soft.
val DaycareTextMuted = Color(0xFF9CA3AF)

// Warna border input saat tidak aktif.
val DaycareBorder = Color(0xFFD1D5DB)

// Warna tombol ketika disabled/loading.
val DaycareDisabled = Color(0xFF9CCFC7)

// Warna background untuk pesan error.
val DaycareErrorBackground = Color(0xFFFFE5E5)

// Warna teks error.
val DaycareErrorText = Color(0xFFB91C1C)

// Warna tambahan untuk aksen hangat.
val DaycareAccent = Color(0xFFF4A261)

// Warna aksen lembut.
val DaycareAccentLight = Color(0xFFFFE8D6)